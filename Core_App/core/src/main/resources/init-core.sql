-- Core Database Initialization Script
-- This script can be used to initialize the core database with sample data

-- Sample comment - add sample data here if needed

-- The tables will be auto-created by JPA/Hibernate
-- You can add initial data for lookup tables or configuration here

-- Reset sequences (run after inserting data)
-- SELECT setval('staff_id_seq', (SELECT MAX(id) FROM staff));
-- SELECT setval('doctors_id_seq', (SELECT MAX(id) FROM doctors));
-- SELECT setval('patients_id_seq', (SELECT MAX(id) FROM patients));
-- SELECT setval('finance_staff_id_seq', (SELECT MAX(id) FROM finance_staff));
-- SELECT setval('radiologists_id_seq', (SELECT MAX(id) FROM radiologists));
